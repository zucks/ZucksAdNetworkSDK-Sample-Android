<?xml version="1.0" encoding="utf-8"?>
<manifest package="net.zucks.zucksadnetworksdk.sample.android"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".BannerActivity">
        </activity>
        <activity android:name=".InterstitialActivity">
        </activity>
        <activity android:name=".BannerNoCallbackActivity">
        </activity>
        <activity android:name=".BannerCallbackActivity">
        </activity>
        <activity android:name=".FullscreenBannerActivity">
        </activity>
        <activity android:name=".FullscreenBannerCallbackActivity">
        </activity>
        <activity android:name=".FullscreenInterstitialActivity">
        </activity>
        <activity android:name=".NativeAdActivity">
        </activity>
    </application>

</manifest>